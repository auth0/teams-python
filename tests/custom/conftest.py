"""Shared fixtures for tests/custom/.

The WireMock container lifecycle is managed by the top-level tests/conftest.py
plugin; wire helpers for the public wrapper live in tests/custom/wire_support.py.
"""
